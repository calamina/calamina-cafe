---
import type { Link } from "../models/Link";
import type { Media } from "../models/Media";
import { query } from "../utils";
import Main from "../layouts/Main.astro";
import LinkList from "../components/LinkList.vue";
import MediaRecent from "../components/MediaRecent.vue";
import Favorites from "../components/about/Favorites.astro";
import Timeline from "../components/Timeline.astro";
import SectionTitle from "../components/SectionTitle.vue";

const links = await query<Link>("aboutLinks");
const medias = await query<Media>("aboutMedia");
---

<Main>
	<div class="content">
		<section class="now">
			<SectionTitle name="now" icon="tabler:mood-smile" />
			<LinkList links={links} />
			<MediaRecent medias={medias} />
		</section>

		<section>
			<SectionTitle name="favorites" icon="tabler:heart" />
			<Favorites />
		</section>

		<section>
			<SectionTitle name="resume" icon="tabler:database" />
			<Timeline />
		</section>
	</div>
</Main>

<style scoped lang="scss">
	.content {
		display: flex;
		flex-flow: column;
		align-items: start;
		padding-top: 2rem;
		gap: 4rem;
		opacity: 1;
		transform: translateY(0);
		transition: transform 0.3s;
	}

	.loader {
		border: 2px solid var(--bg-darker0);
		border-radius: 0.5rem;
		height: 80%;
		width: 30rem;
		height: 30rem;
		position: relative;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	section {
		width: 100%;
	}

	@media (min-width: 1280px) {
		.wrapper {
			flex-flow: row;
		}
	}
</style>
