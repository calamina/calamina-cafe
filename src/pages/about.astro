---
import LinkList from "../components/LinkList.vue";
import MediaRecent from "../components/MediaRecent.vue";
import Main from "../layouts/Main.astro";
import { getCollection } from "astro:content";

const links = await getCollection('aboutLinks').then(links => links.map(link => link.data));
const medias = await getCollection('aboutMedia').then(medias => medias.map(media => media.data));
---

<Main>
	<div class="wrapper">
		<div class="content">
			<LinkList links={links} />
			<MediaRecent medias={medias} />
			<!-- <MediaRecent medias={medias} title="Last media seen" /> -->
		</div>
	</div>
</Main>

<style scoped lang="scss">
	.wrapper {
		display: flex;
		flex-flow: column;
		gap: 0.5rem;
		justify-content: space-between;
		align-items: center;
		width: 100%;
	}

	.content {
		display: flex;
		flex-flow: column;
		align-items: start;
		gap: 2rem;
		opacity: 1;
		transform: translateY(0);
		transition: transform 0.3s;
	}

	.loader {
		border: 2px solid var(--bg-darker0);
		border-radius: 0.5rem;
		height: 80%;
		width: 30rem;
		height: 30rem;
		position: relative;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	section {
		width: 100%;
	}

	@media (min-width: 1280px) {
		.wrapper {
			flex-flow: row;
		}
	}
</style>
