---
interface Props {
  name: string;
}
const { name } = Astro.props;
---

<h1>{name}</h1>

<style>
  h1 {
    text-transform: capitalize;
    font-size: 3rem;
    line-height: 3rem;
    white-space: nowrap;
    letter-spacing: -3px;
    opacity: 1;
    transform: translateX(0);
    transition:
      opacity 0.1s 0.2s ease-out,
      transform 0.2s 0.2s ease-out;
  }

  @starting-style {
    h1 {
      opacity: 0;
      transform: translateX(-0.5rem);
    }
  }
</style>
